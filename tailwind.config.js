/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'media', // or 'media' if you prefer to use the system's dark mode setting
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

